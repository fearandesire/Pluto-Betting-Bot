/**
 * @module oddOrEven -
 * If the number is odd, return 1, if the number is even, return 0.
 * @param {integer} num - The number to check if it's odd or not.
 * @returns The remainder of the number divided by 2.
 */

export function OddOrNot(num) {
	//? Odd = 1, Even = 0
	return num % 2
}
