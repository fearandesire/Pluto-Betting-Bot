/**
 * @summary The type of log message - Controls the color of the log message embed.
 *
 * 0 - Success (green)
 * 1 - Info (blue)
 * 2 - Warning (yellow)
 * 3 - Error (red)
 */
export enum LogType {
	Error = 0,
	Info = 1,
	Warning = 2,
	Success = 3,
}
