/**
 * Prop button types enum
 * Used for reference purposes - actual button IDs are now in format: prop_{outcome_uuid}
 */
export enum PropButtons {
	OVER = 'OVER',
	UNDER = 'UNDER',
	CANCEL = 'CANCEL',
}
